/*
 * Given a sum function, create a sub and mul functions
 * Try copying over as much as possible, minimize the amount of keys pressed
 */

function sum(a, b) {
  return a + b;
}

function sub(a, b) {
  return a - b;
}

function mul(a, b) {
  return a * b;
}
